import React, { Component } from 'react';

export default function ChangeButton(props) {
  return (
    <button
      onClick={props.handleClick}>Placeholder Text</button>
  )
}
//button name should change back to {props.label} when ready
